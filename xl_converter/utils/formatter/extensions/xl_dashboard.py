"""xl_dashboard.py

Extenstion of xl_formatter module that creates a simple summary dashboard.
Should include metadata summary (row counts, fields (selectable?), etc) and
default configurable pivot table. Ability to extend with default visualizations
and forecasting utilities.
"""
